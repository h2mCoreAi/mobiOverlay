# OCR engine used by modules/logistics_hub/module.py.
#
# easyocr is permissively licensed (Apache-2.0), pip-installable, and
# works out‑of‑the‑box on Windows without requiring a separate Tesseract
# binary.  Tradeoff: it's much heavier (pulls in PyTorch + torchvision).
# Keep this at the module level so the base project's dependency list
# stays clean.
easyocr>=1.7.0
Pillow>=9.0.0
